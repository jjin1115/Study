#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <map>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int n, q;
    cin >> n >> q;
    vector<string> hrml;
    vector<string> query;
    hrml.resize(n);
    query.resize(q);

    for(int i = 0; i < n; i++) {
        cin >> hrml[i] ;

    }
    return 0;
}
